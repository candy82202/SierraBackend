@model NiceAdmin.Models.EFModels.LessonCategory

@{
    ViewBag.Title = "Edit";
}

<main id="main" class="main">
    <div class="pagetitle">
        <h1></h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="@Url.Action("Index","Home")">首頁</a></li>
                <li class="breadcrumb-item"><a href="@Url.Action("Index","LessonTags")">課程分類管理</a></li>
                <li class="breadcrumb-item active"><a href="@Url.Action("Edit","LessonTags")">編輯課程分類</a></li>
            </ol>
        </nav>
    </div>
    <!-- End Page Title -->

</main>




@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="row">
        <h4>LessonCategory</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.LessonCategoryId)
        <div class="mb-3">
            @Html.LabelFor(model => model.LessonCategoryName, htmlAttributes: new { @class = "form-label" })
            @Html.EditorFor(model => model.LessonCategoryName, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.LessonCategoryName, "", new { @class = "text-danger" })
        </div>
        <div class="mb-3">
            <input type="submit" value="Save" class="btn btn-primary" />
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
