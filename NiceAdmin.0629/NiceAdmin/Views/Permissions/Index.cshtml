@model IEnumerable<NiceAdmin.Models.EFModels.Permission>

@{
    ViewBag.Title = "Index";
}

<main id="main" class="main">
    <div class="pagetitle">
        <h1>權限清單</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="@Url.Action("Index","Home")">首頁</a></li>
                <li class="breadcrumb-item"><a href="@Url.Action("Index","Members")">會員清單</a></li>
                <li class="breadcrumb-item"><a href="@Url.Action("Index","Employees")">員工清單</a></li>
                <li class="breadcrumb-item"><a href="@Url.Action("Index","Roles")">角色清單</a></li>
                <li class="breadcrumb-item"><a href="@Url.Action("Index","Permissions")">權限清單</a></li>
            </ol>
        </nav>
    </div>
    @*<p>
            @Html.ActionLink("Create New", "Create")
        </p>*@
    <table class="table table-striped table-hover" style="width: 30%;text-align: center ">
        <thead>
            <tr>
                <th>
                    編號
                </th>
                <th>
                    權限
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.PermissionId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PermissionName)
                    </td>
                    @*<td>
            @Html.ActionLink("Edit", "Edit", new { id = item.PermissionId }) |
            @Html.ActionLink("Details", "Details", new { id = item.PermissionId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.PermissionId })
        </td>*@
                </tr>
            }
        </tbody>

    </table>
</main>