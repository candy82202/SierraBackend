@model IEnumerable<NiceAdmin.Models.EFModels.DiscountGroup>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DiscountGroupName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DiscountGroupName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DiscountGroupId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DiscountGroupId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DiscountGroupId })
        </td>
    </tr>
}

</table>
