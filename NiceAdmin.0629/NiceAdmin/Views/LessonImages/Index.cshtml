@model IEnumerable<NiceAdmin.Models.EFModels.LessonImage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Lesson.LessonTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LessonImageName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Lesson.LessonTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LessonImageName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LessonImageId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LessonImageId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LessonImageId })
        </td>
    </tr>
}

</table>
