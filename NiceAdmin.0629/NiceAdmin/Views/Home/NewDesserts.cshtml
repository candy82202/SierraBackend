
@{
    ViewBag.Title = "NewDesserts";
    Layout = "~/Views/Shared/_LayoutFront.cshtml";
}
@model IEnumerable<NiceAdmin.Models.ViewModels.DessertsVM.DessertIndexPartVM>
<main>

    <div class="container mt-5  ">
        <h2 class="w-25 mb-3">新品上市</h2>
        <div class="row">          

                @foreach (var item in Model)
                {<div class="col-xl-3 col-lg-6 col-md-12">
                        <div class="card card1 border-0">
                            <div class="card-img h200px overflow-hidden object-fit-cover">
                                @*<img src="~/Uploads/ @Html.DisplayFor(modelItem => item.DessertImageName)" alt="" />*@
                            </div>
                            <div class="card-body">
                                <a href="" class="cardlink text-decoration-none text-dark stretched-link">
                                    <h5 class="card-title"> @Html.DisplayFor(modelItem => item.DessertName)</h5>

                                </a>
                                @*<div class="card-footer bg-white border-top-0 d-flex">
                <p class="card-text">@dessert.Description</p>
            </div>*@
                                <div class="card-footer bg-white border-top-0 d-flex">
                                    <p class="card-text"> @Html.DisplayFor(modelItem => item.UnitPrice)</p>
                                </div>

                            </div>
                        </div>
                    </div>
                    <!--<tr>

                        <td>-->
                            @*@Html.DisplayFor(modelItem => item.Category.CategoryName)*@
                            <!--@Html.DisplayFor(modelItem => item.CategoryName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.DessertName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.UnitPrice)
                        </td>
                        <td class="status">
                            @if (item.StatusText == "上架")
                            {
                                <span class="badge bg-success">@Html.DisplayFor(modelItem => item.StatusText)</span>
                            }
                            else
                            {
                                <span class="badge bg-danger">@Html.DisplayFor(modelItem => item.StatusText)</span>
                            }
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CreateTime)
                        </td>

                    </tr>-->
                }

            </div>
                        </div>
                   
</main>
